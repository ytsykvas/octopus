{
  "skill_name": "conductor-study",
  "evals": [
    {
      "id": 0,
      "name": "checkpoints",
      "prompt": "Хочу додати чекпоінти, як у Conductor — щоб можна було відкотити код і чат на попередній хід. Подивись, як вони це зробили, і скажи, як нам це реалізувати.",
      "expected_output": "Explains Conductor's checkpoint mechanism from the bundled script — a private git ref, non-disruptive save, temp index for untracked files — labels the evidence, states octopus has none, and turns it into an implementation direction rather than an essay.",
      "assertions": [
        "Names the private ref refs/conductor-checkpoints/<id> as where a checkpoint is stored",
        "States that saving moves neither HEAD nor any file on disk",
        "Describes the second/temporary index used to capture untracked files",
        "Labels which claims come from the bundled script versus the docs versus inference",
        "States that octopus has no checkpoints today",
        "Proposes a concrete direction for octopus rather than only describing Conductor"
      ],
      "files": []
    },
    {
      "id": 1,
      "name": "env-files",
      "prompt": "Коли створюється новий воркспейс, у ньому немає .env і воно не запускається. Як це вирішено в Conductor?",
      "expected_output": "Identifies Files to copy / .worktreeinclude, gives the resolution order, notes only gitignored files are eligible, separates it from a setup script, and says octopus lacks the feature.",
      "assertions": [
        "Names Files to copy and/or .worktreeinclude",
        "Gives the resolution order: .worktreeinclude, then file_include_globs, then the default .env* pattern",
        "States that only files git already ignores are eligible",
        "Distinguishes copying static files from running a setup script",
        "States that octopus has no equivalent today"
      ],
      "files": []
    },
    {
      "id": 2,
      "name": "non-goal-guard",
      "prompt": "У Conductor можна вибрати Codex або Cursor замість Claude Code. Давай і в нас так зробимо — подивись, як у них влаштований вибір агента, і сплануй нам це.",
      "expected_output": "Answers factually how Conductor's harnesses work, but surfaces that multi-agent support is a recorded non-goal in docs/PROJECT.md §5 and §6 before planning anything — then leaves the decision to the user instead of silently building or flatly refusing.",
      "assertions": [
        "Identifies multi-agent support as a declared non-goal, citing docs/PROJECT.md",
        "Still answers factually how Conductor runs four harnesses",
        "Names the concrete architectural cost for octopus, such as the direct Agent SDK integration in agent.ts",
        "Puts the decision back to the user rather than either refusing outright or planning as if nothing were at stake"
      ],
      "files": []
    }
  ]
}
